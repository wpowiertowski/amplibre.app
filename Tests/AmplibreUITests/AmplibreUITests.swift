import Testing
@testable import AmplibreUI

@Suite("AmplibreUI")
struct AmplibreUITests {
    @Test("Placeholder")
    func placeholder() {
        // UI snapshot tests will be added when skin rendering is complete (Week 4)
        #expect(true)
    }
}
